
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GestionaTuMenu" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >@color/violeta_claro</item>
        <!-- Customize your theme here. -->
    </style>

    <style name = "BottomNavigation">
        <item name="android:background">@color/violeta_oscuro</item>
        <item name="itemIconTint">@color/nav_bar_state</item>
        <item name="itemTextColor">@color/nav_bar_state</item>
    </style>

    <style name = "ActionBarMenu" parent="Theme.AppCompat.Light">
        <item name="android:colorBackground">@color/violeta_claro</item>
        <item name="android:textColor">@color/white</item>
        <item name="itemIconTint">@color/white</item>
    </style>

    <style name = "SplashScreem" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:statusBarColor">@color/violeta_oscuro</item>

    </style>

</resources>